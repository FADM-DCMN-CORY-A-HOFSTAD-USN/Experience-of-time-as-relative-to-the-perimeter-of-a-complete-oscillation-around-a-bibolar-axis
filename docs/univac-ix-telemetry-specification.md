# Univac-IX Telemetry and Mathematical Tracking Interface

## 1. System Overview
As part of the Univac-IX architecture, the software stack operates exclusively as a visual telemetry tracker for the physical Relativistic Time-Variance Measurement Apparatus. The software does not simulate the environment; it tracks, calculates, and visually graphs the live mathematical outputs generated by the physical traces (Alpha and Beta) processing hexadecimal signals at the 7.83 Hz Earth-Resonance frequency.

## 2. Mathematical Visualization Vectors
The Univac-IX telemetry dashboard visually tracks three core mathematical calculations executed by the native hardware:

1.  **Angular Synchronization ($T_{sys}$):** Visually tracks the injection of the hexadecimal payload (`0xDEADBEEFCAFEBABE`) at exactly 49.20 rad/s.
2.  **Temporal Variance ($\Delta \tau$):** Calculates and plots the relativistic drift between Trace Alpha and Trace Beta based on the physical radius constraints, visualizing the equation:
    $$ \Delta \tau = T_{sys} \left( \sqrt{1 - \frac{(r_{\alpha}\omega)^2}{c^2}} - \sqrt{1 - \frac{(r_{\beta}\omega)^2}{c^2}} \right) $$
3.  **Accumulated Hardware Fatigue ($F_{total}$):** Visually logs the specific hexadecimal bit-flips and data corruption resulting from the wave compression and material decay at the outer Trace Beta perimeter.

## 3. UI / Dashboard Outputs
The software outputs a continuous command-line or graphical interface detailing the exact mathematical state of the hardware per 0.1277-second execution cycle, allowing Unisys engineers and foundry QA to visually monitor the physical breakdown of the silicon in real-time.
